%h1 Your Tweets
%h3
  Don't see the correct tweets? Enter your username.
%hr
  = form_tag("/") do
    = label_tag(:user_name,"Search for:", class: "twitter-label")
    = text_field_tag(:user_name, "", class: "form-control", placeholder: "Username")
    = submit_tag("Search",class: 'btn btn-primary btn-xl submit-tweets')